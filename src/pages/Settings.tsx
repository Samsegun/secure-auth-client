import PageHeader from "@/components/customUi/PageHeader";
import PageWrapper from "@/components/customUi/PageWrapper";

function Settings() {
    return (
        <PageWrapper>
            <PageHeader>Settings (for Admins)</PageHeader>
        </PageWrapper>
    );
}

export default Settings;
